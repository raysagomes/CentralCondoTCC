# 🧪 Testes

Esta pasta contém todos os testes do projeto usando Jest e Testing Library.

## Estrutura

- `__tests__/components/` - Testes de componentes React
- `__tests__/hooks/` - Testes de hooks customizados
- `__tests__/services/` - Testes de serviços e APIs
- `__tests__/utils/` - Testes de funções utilitárias

## Scripts

```bash
# Executar todos os testes
npm test

# Executar testes em modo watch
npm run test:watch

# Executar testes com coverage
npm run test:coverage
```

## Configuração

- `jest.config.js` - Configuração principal do Jest
- `jest.setup.js` - Setup global para os testes